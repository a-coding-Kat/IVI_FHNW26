/* WebGazer is loaded at runtime from CDN by tracking.js — see that file.
   This local stub exists only to neutralise the broken auto-loaded copy
   that Dash would otherwise serve from the assets/ folder. */
console.debug("[gaze] webgazer.js stub loaded — real library injected by tracking.js");
